export enum OpenPhaseDialog {
  None,
  Scenario,
  NightOrder,
}

export enum OpenBoardDialog {
  None,
  Dismiss,
  ViewPlayer,
}

export enum OpenCharacterDialog {
  None,
  Module,
  VoteHistory,
}